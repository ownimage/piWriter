import { NumberToXbPipe } from './number-to-xb.pipe';

describe('NumberToXbPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToXbPipe();
    expect(pipe).toBeTruthy();
  });
});
