import { DecimalToPercentagePipe } from './decimal-to-percentage.pipe';

describe('DecimalToPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalToPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
